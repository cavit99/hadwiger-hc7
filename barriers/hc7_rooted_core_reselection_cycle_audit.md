# Audit of the rooted-core reselection cycle

## Verdict

**GREEN**, after making seven-connectivity explicit in the general
`K_2 vee P` paragraph.

The checker verifies the two displayed rooted `K_4` models, their common
carrier and profile, all 3,168 crossing root choices over the twelve actual
order-seven cuts, and the next pentagonal-tube probe.

The host is seven-connected and `K_7`-minor-free: removing the at most two
model bags using the universal vertices from a putative `K_7` model would
leave a `K_5` minor in the planar icosahedron.  Swapping the apices is a
literal involution fixing the roots, deleted edges and separation.  The
neutral-cycle conclusion is therefore sound.

For the general joined-planar observation, seven-connectivity makes the
base five-connected.  After universal-apex assignment, a path between the
two boundary roots in the base with the two shore roots deleted supplies
the final rooted-bag adjacency.  Three full packets would contract in the
planar base to a forbidden `K_{3,5}` minor, so the packet claim is sound.
