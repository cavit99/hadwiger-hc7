# Split note for the merged-response Kempe/cycle draft

An uncommitted combined draft was split after scope audit because the
repository does not currently reach its two main hypothesis families on one
physical shore.

Its proved content now appears as:

- `results/hc7_merged_root_three_kempe_locks.md`; and
- `results/hc7_adjacent_full_pair_cycle_completion.md`.

The combined draft at SHA-256
`bfd1315a405b32889ce741d6c873094f53adfac513f6a26710ad479c77c84628`
received a GREEN independent internal audit at SHA-256
`18a62691abdec04c921b5e89f3cded038f98567e00d3c855d72da24350f55b6a`
before it was split.  It also recorded the following diagnostic
conditional.  If the merged-root
open shore is the disjoint union of connected subgraphs `A_d,A_e` missing
exactly `d,e`, respectively, then each of the three colour-indexed root
paths crosses between those two subgraphs.  Selecting one crossing edge
per absent colour gives three distinct edges with three distinct
private-colour endpoints; their common-colour endpoints need not be
distinct.  This statement is valid but is not known to arise in the live
branch.  The full combined draft was therefore not promoted.
