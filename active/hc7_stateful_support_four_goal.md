# Active goal: stateful support-four composition

**Status:** research target, not a proved theorem.

## Decision

The next proof goal is to close the residual state-composition edge in the
connected-rich paired width-two `(1,2)` cell.  The compulsory atomic bridge
is the nondecreasing base case of this composition theorem; it is not the
whole goal.

This replaces two unproductive formulations:

* a geometry-only packet escape is false; and
* declaring the compulsory bridge ends to be the final apex pair is false.

The missing mechanism must couple literal routing to the universal
proper-minor colouring responses.

## Target theorem

Let `G` satisfy the frozen hypothetical-counterexample kernel.  Assume an
actual separation

\[
 V(G)=L\mathbin{\dot\cup}S\mathbin{\dot\cup}R,
 \qquad |S|=7,
\]

with no `L-R` edges, oriented packet vector
`nu_L=1, nu_R=2`, connected rich shore `R`, and the paired state

\[
 \Pi=\{\{a_1,t_1\},\{a_2,t_2\},\{a_3,t_3\},\{c\}\},
\]

legally attained by the named thin-side proper-minor operation.  Assume the
audited paired incidences and width-two frontier, and a three-cut of the
three-connected thin shore having exactly two lobes, one of which is `D`
with `|N_S(D)|=4`.  In the capacity graph of the two lobes and three gate
vertices, assume the matching number is one.  The gate-centred star is
excluded by the audited five-cut argument.  For a nonsingleton selected
lobe, assume the proper-row outcome of the literal portal peel; its full-row
outcome is already closed.  The singleton-lobe outcome remains in scope.
These are the exact two-lobe proper-row/singleton residues of the audited
capacity-transition and portal-peel theorems.

Prove that at least one of the following occurs.

1. A partition belongs to both literal closed-shore extension languages

   \[
     \operatorname{Ext}(G[L\cup S],S)
     \cap
     \operatorname{Ext}(G[R\cup S],S),
   \]

   so palette alignment six-colours `G`.  A clique-reservoir two-carrier
   return is one way to obtain this outcome.
2. There is a literal `K_7` model or a fixed pair `{p,q}` such that
   `G-{p,q}` is `K_5`-minor-free.
3. There is an actual seven-adhesion `(L',Omega,R')`, oriented with
   `nu_{L'}=1, nu_{R'}=2`, such that `|L'|<|L|`.  The construction supplies
   the literal old-to-new boundary map and a named proper-minor operation
   attaining on `Omega` a state of the same `2+2+2+1` paired form, with the
   same paired incidences and a width-two frontier.  After the
   already-audited low-cut, component, and four-connected normalizations,
   it either reaches outcomes 1--2 or re-enters this same support-four class
   with oriented thin-shore order at most `|L'|<|L|`.

An unlabelled `K_7^\vee` minor, an unspecified `(1,1)` adhesion, or an
unaccompanied smaller separator is not an outcome of the theorem.  Those
objects belong to the still-open `S1/S4` programmes and cannot be used to
declare this goal met.

## First mandatory milestone: one-branch bridge-state lift

Use the atomic compulsory-portal setup with unique bridge `e=zu`.  A
witnessed tuple consists of one of the legally attained blocking trace
colourings, one shortest blocking path `L_0` in that colouring (of either
audited endpoint type), two disjoint edge-minimal connected `S`-full packet
trees `P_0,Q_0`, and an inclusion-minimal connected rich-shore subgraph `B`
containing
`P_0 union Q_0 union (L_0-S)`.  Among all witnessed tuples, select one
minimizing

\[
                 (|V(B)|,|E(B)|,|E(L_0)|)
\]

lexicographically over all witnessed tuples.  Fix any tuple attaining this
minimum; the theorem must hold for every minimizing tuple.  The first
milestone covers the case in which its `B` is a subdivision of a tree with
maximum degree at most three and at most one vertex of degree three:
equivalently, an arbitrary path or `Y` core.  Only the exact frozen
four-rich-vertex path architecture from the audited
`8,192`-extension classification may be removed as already completed;
longer zero-branch paths remain in scope.

For a six-colouring `phi`, let `pi_S(phi)` be its equality partition on the
literal boundary.  Define

\[
 \mathcal D_e=
 \{\pi_S(\phi):\phi\in\operatorname{Col}_6(G-e)\}
\]

and, since every edge of `B` lies wholly in the rich open shore,

\[
 \mathcal O_B=
 \bigcup_{f\in E(B)}
 \left(
  \{\pi_S(\psi):\psi\in\operatorname{Col}_6(G-f)\}
  \cup
  \{\pi_S(\psi):\psi\in\operatorname{Col}_6(G/f)\}
 \right).
\]

The critical-pinch theorem already puts every six-colouring of `G-e` on the
literal seven-adhesion.  Every such live state has packet demand at least
three: if its demand were at most two, the two rich full packets would
reproduce it on the intact thin closed shore, contradicting the
critical-pinch nonextension conclusion.  Compare this state family with
the deletion/contraction family `O_B`.

Prove one of:

* `D_e cap O_B` is nonempty, which gives the opposite-operation collision
  and six-colours `G`;
* a legal reassignment of two named disjoint adjacent connected thin-side
  carriers whose recomputed literal contact lists are list-colourable,
  equivalently satisfy the common-orientation success side of the audited
  two-carrier criterion, and hence yield target outcome 1;
* a literal `K_7` or the fixed-pair terminal; or
* an actual `(1,2)` exact-seven descent with smaller oriented thin shore,
  explicit boundary map, and legally attained recursive state.

This is the first uniform extension of the completed four-vertex
packet-path cell.  Do not enlarge to two branch vertices until the arbitrary
path-or-`Y` milestone is proved or a counterexample satisfying every frozen
literal hypothesis is certified.

## Research constraints

1. Use `K_7`-minor-freeness, seven-connectivity, Dirac's local inequality,
   and universal deletion/contraction responses together.  Abstract
   boundary-state data alone are insufficient.
2. Humeau--Pous webs may supply a literal routing certificate, but completion
   edges are never host edges and the web theorem does not transport a
   colouring state.
3. A Pap-style auxiliary graph is admissible only after one first-hit move
   is encoded by a total permutation while preserving literal vertex
   capacity.  Otherwise retire that encoding.
4. Rooted folios may certify bounded carrier patterns but do not preserve
   exact colour-extension languages or the orientation bit.
5. No new finite boundary census is part of this goal.  Computation is for
   falsification and independently checkable certificates only.

## Success and stopping tests

The goal succeeds only when the target theorem is proved and independently
audited.  The first milestone is falsified if a labelled path-or-`Y` atom is
found which satisfies the entire frozen kernel, has
`D_e cap O_B=emptyset`, has no legal carrier reassignment satisfying the
list criterion, has no terminal outcome, and has no strict state-carrying
descent.  A barrier missing seven-connectivity, `K_7`-minor-freeness, or full
minor-criticality does not falsify the target.

Even success leaves arbitrary demand-three `(1,2)` states, `(1,1)`, larger
adhesions, and global `S1` rotation coherence.  It would close a genuine
infinite family and a named proof-spine edge, not `HC_7` by itself.

## Literature boundary

The closest current tools do not supply the theorem directly:

* [Humeau--Pous](https://arxiv.org/abs/2505.16431) gives an uncoloured
  two-path/web dichotomy and recursive web composition.
* [Pap](https://doi.org/10.1007/s00493-007-0056-z) gives a min--max theorem
  for non-returning paths in a genuinely permutation-labelled graph.
* [Cavallaro--Gorsky--Kreutzer--Thilikos--Wiederrecht](https://arxiv.org/abs/2605.14902)
  preserves bounded rooted folios, not equality-state languages.
* [Dvořák--Swart](https://arxiv.org/abs/2504.07764) shows that abstract
  boundary-colouring languages can be realized with great freedom, so the
  literal critical geometry is indispensable.
